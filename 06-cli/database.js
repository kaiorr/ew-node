const {
  readFile
} = require('fs')

const { promisify } = require('util')
const readFileAsync = promisify(readFile)

const Database {
  constructor() {
    this.NOME_ARQUIVO = 'herois.json'
  }
}

class Database {
  obterDadosArquivo({

  })

  escreverArquivo({

  })

  listar(){
    return null
  }
}